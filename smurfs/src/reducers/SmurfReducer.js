export const initialState = {
  smurfsArray: [],
  isGetting: false,
  isPosting: false,
  errors: ""
};

export const smurfsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
